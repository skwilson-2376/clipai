# ── Local MySQL (Docker) ──────────────────────────────────────────────────────
DATABASE_URL=mysql://clipai:clipai_dev@localhost:3306/clipaiapp

# ── Azure MySQL Flexible Server ───────────────────────────────────────────────
# DATABASE_URL=mysql://clipaiadmin:<PASSWORD>@<SERVER>.mysql.database.azure.com/clipaiapp?ssl-mode=require

# ── Server ────────────────────────────────────────────────────────────────────
PORT=8080
RUST_LOG=clipai_backend=info,tower_http=info
